# Streamlit Secrets Configuration Example
# Duplicate this file as 'secrets.toml' inside '.streamlit/' and insert your NVIDIA API Key.
# Obtain a free key from the NVIDIA API Catalog (https://build.nvidia.com).
# NOTE: Never commit your actual 'secrets.toml' file to version control.

NVIDIA_API_KEY = "your-nvidia-api-key-here"
